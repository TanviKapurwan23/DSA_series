/*
A function that calls itself...
...until it doesn't

openGiftBox() function

Pseudo Code:

* The process of opening each new box is same.
* Each time we open a box, we make the problem smaller.

Ball* openGiftBox(){
    // Ball* ball = new Ball();
    if(isBall) return ball;
    openGiftBox();
}


Base Case 

Recursive Case

should not a infinite loop


*/




